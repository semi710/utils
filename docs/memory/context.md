# LLM Context

## Goal
Personal utility scripts packaged as a Nix flake. Extracted from ndots for reuse across machines.

## State
- Packages: aerospace, hypr-clients, rofi (fullmenu/menus/powermenu), walogram, waybar, yabai, system, external, hypr-scripts
- Flake exposes packages per-system, importable via flake input
- Originally inline scripts in ndots, moved to standalone repo for editability
